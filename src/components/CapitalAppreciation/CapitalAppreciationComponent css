.input-wrapper {
  display: flex;
}
table {
  border-collapse: collapse;
  width: 100%;
}

th,
td {
  text-align: left;
  padding: 0.2em;
}

tr:nth-child(even) {
  background-color: var(--main-font-colour);
  color: var(--main-bg-colour);
}
